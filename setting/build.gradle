apply from: '../commonBuild.gradle'

android {
    defaultConfig {
        if (!isRelease) {
            applicationId "com.example.setting"
            versionCode 1
            versionName "1.0"
        }
    }
}