apply from: '../commonBuild.gradle'

android {
    defaultConfig {
        if (!isRelease) {
            applicationId "com.example.homepage"
            versionCode 1
            versionName "1.0"
        }
    }
}